From: Claude Security Review <security@anthropic.com>
Date: Sat, 25 Jan 2026 00:00:01 +0000
Subject: [PATCH 2/11] security: Replace unsafe UID call with nix crate (HIGH)

Replace unsafe { libc::getuid() } with nix crate's safe wrapper to
eliminate unsafe FFI call.

Severity: HIGH
Impact: Removes unsafe block, uses audited safe wrapper
---
 rust/secrets-verify/Cargo.toml | 1 +
 rust/secrets-verify/src/main.rs | 2 +-
 2 files changed, 2 insertions(+), 1 deletion(-)

diff --git a/rust/secrets-verify/Cargo.toml b/rust/secrets-verify/Cargo.toml
index 1234567..abcdefg 100644
--- a/rust/secrets-verify/Cargo.toml
+++ b/rust/secrets-verify/Cargo.toml
@@ -15,3 +15,4 @@ anyhow = { workspace = true }
 walkdir = { workspace = true }
 which = { workspace = true }
 dirs = { workspace = true }
+nix = { workspace = true }
diff --git a/rust/secrets-verify/src/main.rs b/rust/secrets-verify/src/main.rs
index 1234567..abcdefg 100644
--- a/rust/secrets-verify/src/main.rs
+++ b/rust/secrets-verify/src/main.rs
@@ -146,7 +146,7 @@ fn check_secret_permissions(path: &Path, name: &str) -> Result<bool> {

         // 2. Check file ownership (must be owned by current user)
         let file_uid = metadata.uid();
-        let current_uid = unsafe { libc::getuid() };
+        let current_uid = nix::unistd::Uid::current().as_raw();
         if file_uid != current_uid {
             security_checks_passed = false;
             eprintln!(
--
2.45.0
